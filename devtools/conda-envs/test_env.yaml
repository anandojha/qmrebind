name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - ambertools 
  - biopandas 
  - pandas 
  - matplotlib 
  - regex
  - versioneer
  # - openmm

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - codecov
    - parmed

