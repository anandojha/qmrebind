name: docs
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - ambertools
  - biopandas
  - pandas
  - matplotlib
  - parmed
  - regex
  - pip

  - pip:
    - sphinx-autoapi
    - PyPDF2
